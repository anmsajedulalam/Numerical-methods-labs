function yy=Lagrange_int(x,y,xx)
n=length(x);
if length(y)~=n, error('x and y must have same lengths');
end
for p=1:length(xx)
    s=0;
    for i=1:n
        product=y(i);
        for i=1:n
            if i~=j
                product=product*(xx(p)-x(j))/(x(i)
